<% if user_signed_in? %>
  <%= link_to('Sign Out', destroy_user_session_path, :method => :delete, :'data-ajax' => 'false') %>        
<% else %>
  <%= link_to('Sign In', new_user_session_path)  %>  
<% end %>