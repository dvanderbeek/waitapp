<p>
  <b>Name:</b>
  <%= @party.name %>
</p>

<p>
  <b>Phone:</b>
  <%= @party.phone %>
</p>

<p>
  <b>Wait:</b>
  <%= @party.wait %>
</p>

<p>
  <b>Size:</b>
  <%= @party.size %>
</p>

<%= link_to 'Notify', create_text_path(:party_id => @party.id) %> |
<%= link_to 'Delete', party_path(@party), :method => :delete %> |

<%= link_to 'Edit', edit_party_path(@party) %> |
<%= link_to 'Back', parties_path %>
